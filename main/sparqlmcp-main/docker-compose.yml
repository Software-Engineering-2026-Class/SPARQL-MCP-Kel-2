version: "3.8"
services:
  fuseki:
    image: secoresearch/fuseki:latest
    container_name: cskg_fuseki
    ports:
      - "3030:3030"
    environment:
      - ADMIN_PASSWORD=admin123
    volumes:
      - fuseki_data:/fuseki/databases
    restart: unless-stopped
volumes:
  fuseki_data:
