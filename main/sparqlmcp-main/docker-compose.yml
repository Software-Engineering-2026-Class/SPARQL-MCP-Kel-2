version: "3.9"
services:
  fuseki:
    image: stain/jena-fuseki:latest
    container_name: fuseki
    ports:
      - "3030:3030"
    environment:
      ADMIN_PASSWORD: "admin123"
    volumes:
      - fuseki-data:/fuseki
    restart: unless-stopped
volumes:
  fuseki-data:
